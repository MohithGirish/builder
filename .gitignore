# Claude Code — never commit local AI assistant state
.claude/
CLAUDE.md

# Dependencies
node_modules/
**/node_modules/
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Build output
dist/
build/
*.egg-info/
.eggs/

# Environment & secrets
.env
.env.*
!.env.example
*.pem
*.key

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Docker
docker-compose.override.yml

# Coverage / test artefacts
coverage/
.nyc_output/
htmlcov/
.pytest_cache/
.mypy_cache/

# Graphify — local knowledge graph, never push
graphify-out/

# Design system — local AI design reference, never push
design-system/

# Tech stack reference — local only, never push
tech_stack/

# Playwright MCP — local browser automation state, never push
.playwright/
.playwright-mcp/

# WhatsApp / Meta setup guides — contain internal setup notes, keep local only
whatsapp-meta-setup.md
meta-account-setup.md

# Production auth hardening — internal deferred-TODO checklist, keep local only
PRODUCTION_AUTH_HARDENING.md

# Design-sync — staged converter scripts, build output, and machine state (never commit)
.ds-sync/
ds-bundle/
.design-sync/
frontend/.ds-build/

# UI documentation — local generated reference + its generator, never push
ui-documentation.xlsx
ui-documentation-assets/
build_ui_doc.py

# Misc
*.tsbuildinfo
.eslintcache

# Runtime data — quote requests contain requester PII, never commit
backend/data/quotes.json

# Claude working artifacts — impeccable state, session-continuity and generated reference docs, never push
.impeccable/
DESIGN.md
PRODUCT.md
UI_GRADING_PROGRESS.md
TECH_STACK.md
