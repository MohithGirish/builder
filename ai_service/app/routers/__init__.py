"""
routers/__init__.py — Package marker for the API routers sub-package.

This package contains FastAPI APIRouter instances that define the HTTP
endpoints for the matchmaking service. Currently exports the match router
which handles score, rank, and batch-score endpoints under /api/v1/match.
"""
